[dgx]
gx10.local ansible_user=devjoe

; Fallback to IP if mDNS flakes on your LAN; comment the line above and
; uncomment the one below. ansible_user should match the Linux account
; with sudo privileges on the DGX Spark.
; 192.168.99.2 ansible_user=devjoe
