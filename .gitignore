*.retry
*.log
.vault_pass
.vault_password

# Vault file is committed encrypted, but never the plaintext working copy.
group_vars/dgx.yml.vault.plain
group_vars/dgx.yml.vault.bak
.DS_Store
__pycache__/
*.pyc

# Runtime-only generated corpora and local caches.
tmp/

# Local-only raw model experiment artifacts. Keep curated summaries in docs;
# force-add a specific run only when it is intentionally safe to publish.
reports/
reports/qwen-conditional-prompt-gate-*/
reports/qwen-dir-steering-*/

# Local-only overrides
inventory.local.ini
group_vars/local.yml

# Untracked working dirs
scratch/
.archive/
.claude/
